package com.itranswarp.sample;

import java.util.Arrays;

public class Solution {
    public int arrayPairSum(int[] nums) {
        //之理解了一种基于排序的想法，而还有两种，分别是暴力使用permute(nums,indexs)和使用额外空间的想法
        /**int sum = 0;
        Arrays.sort(nums);
        for(int i = 0;i< nums.length;i+=2){
            sum=nums[i];
        }
        return sum;*/
    }
}
